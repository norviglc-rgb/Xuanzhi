# BOOTSTRAP-CHECKLIST

- Confirm active runtime paths match `openclaw.json`
- Separate runtime truth from `Xuanzhi-Dev/legacy-root/` and `Xuanzhi-Dev/generated/`
- Define generic agent creation rules in `agent-smith`
- Define runtime execution rules for agent and user provisioning in `ops`
- Verify existing system workspaces are complete and consistent
- Define which state is live and which files are only seeds or examples
- Verify critic review can be attached to provisioning results
- Verify one daily-user provisioning flow end to end
- Verify one complex-development routing flow end to end
