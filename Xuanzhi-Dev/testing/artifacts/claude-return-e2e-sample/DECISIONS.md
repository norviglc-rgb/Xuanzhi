# DECISIONS

## Decision 1
Use a minimal but schema-aligned `TASK.json` to satisfy complex handoff constraints.

## Decision 2
Keep the sample package under `Xuanzhi-Dev/testing/artifacts` to avoid polluting runtime root.
