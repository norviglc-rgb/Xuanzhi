# ACCEPTANCE

## Criteria
- All required handoff files exist.
- `TASK.json` includes `type=complex_development`, `targetAgent=claude-code`, `runtime=acp`.
- Return package includes `EXECUTION_REPORT.md` with required sections.

## Result
- Current sample meets all criteria.
