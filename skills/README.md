# Skills Directory

Managed shared skills for OpenClaw live here.

- `xuanzhi-control/` contains machine-readable control and workflow-placement rules.
- `agent-smith-daily-user-materialization/` defines the daily-user runtime materialization skill contract owned by `agent-smith`.
- Workspace-local overrides should live in `<workspace>/skills/`.
